import { message } from 'antd';

export const claim = () => {
  (async () => {
    await message.info('claim interface');
  })();
};
export const stake = () => {
  (async () => {
    await message.info('stake interface');
  })();
};
export const withdraw = () => {
  (async () => {
    await message.info('withdraw interface');
  })();
};
